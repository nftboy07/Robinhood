# VPS SYNC COMMANDS (LIVE MAINNET - dry mode completely removed)
#
# For MANY MORE commands that produce REAL OUTPUT (discovery, logs, chain data, TG tests):
#   See COMMANDS.md in the repo (highly recommended)
# Run these AFTER you SSH into the VPS yourself with your PEM key.
#
# Example SSH (use your actual PEM path):
#   ssh -i ~/.ssh/your_robinhood_vps.pem ubuntu@3.69.242.140
#
# Once logged in on VPS:

cd ~/robinhood-bot

# Pull the latest (dry removed, live mainnet)
git pull origin main

# Verify files
ls -l robinhood_bot.js config.json.example .env* 2>/dev/null || true

# IMPORTANT: Your .env and config.json are preserved (gitignored)
# - Edit if needed:
#   nano .env          # confirm PK, TELEGRAM_TOKEN, ADMIN_CHAT_ID
#   nano config.json   # set REAL factory, weth, router (use discover.js output or devtools)
#                      # Run: node discover.js while a launch happens on the site for candidates.
#                      # Copy the printed "READY CONFIG SNIPPET" and edit the addresses.

# Install/update deps if package changed
npm install --production

# Restart PM2 (name from ecosystem.config.js)
pm2 restart robinhood-sniper --update-env

# Check logs
pm2 logs robinhood-sniper --lines 50

# To see status in TG: send /menu or /s to your bot
# New in this update: tx links in alerts, configurable snipe size, better positions list

echo "Bot should now be running LIVE MAINNET with no dry-run code path at all."

# If first time or issues:
# pm2 start ecosystem.config.js --update-env
# pm2 save

# Recommended next (copy paste these for real output):
# 1. Set real factory etc in config.json (use node discover.js or node vps_diag.js)
nano config.json

# 2. Test TG and get real diagnostics
node tg_diag.js
node vps_diag.js

# 3. Restart
pm2 restart robinhood-sniper --update-env

# 4. In TG send:
# /menu
# /d   or /diag   <--- gives current block, balance, factory status etc (real output)
# /poll

# 5. Fund wallet (address printed in logs or /s)
# 6. Watch for launches or use /poll

# Full one-liner check after pull:
# cd ~/robinhood-bot && git pull && npm i --prod && pm2 restart robinhood-sniper --update-env && node vps_diag.js

# Monitor first real activity and report back logs or tx if wanted.
